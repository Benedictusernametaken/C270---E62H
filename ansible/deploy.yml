---

- name: Ensure Docker and dependencies are installed
      ansible.builtin.apt:
        name:
          - docker.io
          - docker-compose-v2
          - rsync
        state: present
        update_cache: yes
- name: Deploy Nutritrack Application via Docker Compose
  hosts: webservers
  gather_facts: yes
  become: yes

  tasks:
    - name: Ensure the permanent application folder exists
      ansible.builtin.file:
        path: /opt/nutritrack
        state: directory
        mode: '0755'
        owner: root
        group: root

    - name: Synchronize repository context (excluding heavy virtual environment files)
      ansible.posix.synchronize:
        src: "{{ playbook_dir }}/../"
        dest: /opt/nutritrack/
        delete: yes  # Automatically cleans up deleted files from the target directory
        rsync_opts:
          - "--exclude=.git"
          - "--exclude=.venv"
          - "--exclude=env"
          - "--exclude=__pycache__"
          - "--exclude=.pytest_cache"

    - name: Pull latest images and spin up the Docker Compose container network
      community.docker.docker_compose_v2:
        project_src: /opt/nutritrack
        state: present
        pull: always
        recreate: always   # ◄ Forces Docker to shift containers from "Created" to "Runnin